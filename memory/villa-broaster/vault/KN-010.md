---
id: KN-010
type: Knowledge
tier: Project
status: Current
impact: High
priority: High
lifetime: Project
created: 2026-08-20T00:00:00.000Z
updated: 2026-08-20T00:00:00.000Z
---

# KN-010 — SISTEMA EN VIVO (2026-08-20): (A) ÓRDENES POLLING INTELIGENTE: panel Órdenes (br

**fact:** SISTEMA EN VIVO (2026-08-20): (A) ÓRDENES POLLING INTELIGENTE: panel Órdenes (broaster-app /admin, pestaña 1) hace polling de 8s SOLO cuando rango=HOY y pestaña VISIBLE (document.visibilityState='visible'). Cada GET /api/admin/ordenes relee el JSON de disco fresco. Señal de orden nueva: badge contador en tab + filas resaltadas (--animate-entrada) + título dinámico '(N nuevas)' + beep WebAudio (armado tras primer gesto usuario — autoplay bloqueado sin interaccion, fallback = señal visual garantizada). API /api/admin/ordenes acepta desde/hasta opcionales (formato días calendario, hasta INCLUSIVE convertido a exclusivo en _lib/rango.ts, default=HOY). (B) CONFIRMACIÓN VILLA-APP EN VIVO: villa-app/components/orden/Confirmacion polling 10s reusando su carga OrderDetail, PARA cuando estado terminal (entregada/cancelada) y REDUCE polling a 60s con pestaña oculta (visibility handling integrado). (C) CATÁLOGO COMPLETO: anexarProducto implementa TERCER uso patrón leer→decidir→escribir SERIALIZADO (sin carrera). Unicidad slug verificada con 10 altas simultáneas: id=prefijo(categoría) + nombre normalizado (trim/lower/kebab), colisión→sufijo -2/-3. validarEntradaProducto obligatorio: nombre 3..80 chars, categoría required, precios enteros >0 AMBAS SEDES (L1/L2), desc ≤200 chars. editarProducto por PATCH parcial (atualiza solo campos presentes). POST /api/admin/productos (anexar) + PATCH /api/admin/productos/[id] (editar). (D) DISEÑO UNIFICADO: system/ui.ts (components/admin/ui.ts) define recetas presentacionales puras — TARJETA(titulo,numero,delta), FILA(items), BOTON_{PRIMARIO,SECUNDARIO,DANGER}(label,onClick), claseChip(estado), CAMPO(nombre,value,onChange), ESTADO_VACIO(icono,titulo,desc). Tokens globales: --shadow-tarjeta (box-shadow sutil), --animate-entrada (fade+slide 400ms). UI de alta colapsable (toggle ⊞) + edición inline en ProductosPanel. Suite: 68 tests (dominio+almacén+acceso).

**context:** Construido run 2026-08-20 (ola A órdenes + ola B catálogo paralelas + ola C confirmación + ola D UI + ola E visual). QA 8/8 PASS, 0 fix-cycles (auto-corrección builder D al detectar bug HTML5 step>1). Verifier resiliente: renanudó vía SendMessage tras 2 muertes infra. Patrón sembrador (order/gasto/producto en disco) permitió reconfirmar sin repetir e2e.

**Referenciado por:** [[KN-005]]

